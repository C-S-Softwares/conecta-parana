# Database
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/conecta_parana?schema=public

# Server
PORT=3000
NODE_ENV=development

# Security
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
CORS_ORIGIN=http://localhost:4200,https://admin.conecta.com,https://app.conecta.com

# JWT — gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Redis (cache)
REDIS_URL=redis://localhost:6379

# GlitchTip — APM / Error Tracking
# DSN gerado no dashboard do GlitchTip (ver docs/GLITCHTIP_SETUP.md)
GLITCHTIP_DSN=
